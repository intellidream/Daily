<UserControl
    x:Class="Daily_WinUI.Controls.HealthTelemetryWidgetControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:chart="using:Syncfusion.UI.Xaml.Charts"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded"
    Unloaded="UserControl_Unloaded">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Border Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="12" Padding="12,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <FontIcon Glyph="&#xEC0A;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="White" Margin="0,0,8,0" />
                    <TextBlock Grid.Column="1" Text="Health" FontSize="14" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="120" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Steps and HR summary -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Steps -->
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE7EE;" FontFamily="Segoe Fluent Icons" FontSize="16" Opacity="0.5" Margin="0,0,0,4" />
                        <TextBlock Text="{x:Bind TotalSteps, Mode=OneWay}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                        <TextBlock Text="steps" FontSize="10" Opacity="0.5" HorizontalAlignment="Center" />
                    </StackPanel>

                    <Border Grid.Column="1" Width="1" Background="{ThemeResource AppGlassBorderColorBrush}" Margin="0,4" />

                    <!-- Sleep -->
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE708;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="#7C4DFF" Margin="0,0,0,4" />
                        <TextBlock Text="{x:Bind TotalSleep, Mode=OneWay}" FontSize="20" FontWeight="Bold" Foreground="#7C4DFF" HorizontalAlignment="Center" />
                        <TextBlock Text="sleep" FontSize="10" Opacity="0.5" HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>

                <!-- Heart Rate Sparkline -->
                <Border Grid.Row="1" Background="{ThemeResource AppGlassColorBrush}" CornerRadius="8" Padding="8" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="HEART RATE" FontSize="10" FontWeight="Bold" Opacity="0.5" Margin="4,0,0,4" />
                        
                        <chart:SfCartesianChart Height="40" Margin="0" Background="Transparent">
                            <chart:SfCartesianChart.XAxes>
                                <chart:DateTimeAxis ShowMajorGridLines="False" />
                            </chart:SfCartesianChart.XAxes>
                            <chart:SfCartesianChart.YAxes>
                                <chart:NumericalAxis ShowMajorGridLines="False" />
                            </chart:SfCartesianChart.YAxes>
                            <chart:SplineSeries ItemsSource="{x:Bind HeartRateData, Mode=OneWay}" XBindingPath="StartTime" YBindingPath="Value" Fill="#FF5252" />
                        </chart:SfCartesianChart>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
