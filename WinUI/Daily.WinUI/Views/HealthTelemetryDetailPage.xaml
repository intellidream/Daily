<Page
    x:Class="Daily_WinUI.Views.HealthTelemetryDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:chart="using:Syncfusion.UI.Xaml.Charts"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="110" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Bar with Icon, Title -->
        <Grid Grid.Row="0" Padding="20,0" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <Border Width="40" Height="40" CornerRadius="20" Background="{ThemeResource AppGlassColorBrush}">
                    <FontIcon Glyph="&#xEC0A;" FontFamily="Segoe Fluent Icons" FontSize="18" Foreground="{ThemeResource AppFgColorBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Health Telemetry" FontSize="32" FontWeight="SemiBold" FontFamily="Segoe UI Variable Display" Foreground="{ThemeResource AppFgColorBrush}" />
                    <TextBlock Text="Detailed granular health data from devices" FontSize="12" Foreground="{ThemeResource AppFgMutedColorBrush}" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24" Padding="20,16,20,20">

                <!-- HEART RATE CHART -->
                <Border Background="{ThemeResource AppGlassColorBrush}" BorderBrush="{ThemeResource AppGlassBorderColorBrush}" BorderThickness="1" CornerRadius="16" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                            <FontIcon Glyph="&#xEB51;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="#FF5252" />
                            <TextBlock Text="Heart Rate Today" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>

                        <chart:SfCartesianChart Height="250">
                            <chart:SfCartesianChart.XAxes>
                                <chart:DateTimeAxis ShowMajorGridLines="False" LabelFormat="HH:mm" />
                            </chart:SfCartesianChart.XAxes>
                            <chart:SfCartesianChart.YAxes>
                                <chart:NumericalAxis ShowMajorGridLines="True" Minimum="40" Maximum="200" Interval="20" />
                            </chart:SfCartesianChart.YAxes>

                            <chart:SplineSeries ItemsSource="{x:Bind HeartRateData, Mode=OneWay}" XBindingPath="StartTime" YBindingPath="Value" Fill="#FF5252" Stroke="#FF5252" StrokeWidth="2" />
                        </chart:SfCartesianChart>
                    </StackPanel>
                </Border>

                <!-- SLEEP STAGES TIMELINE -->
                <Border Background="{ThemeResource AppGlassColorBrush}" BorderBrush="{ThemeResource AppGlassBorderColorBrush}" BorderThickness="1" CornerRadius="16" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                            <FontIcon Glyph="&#xE708;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="#7C4DFF" />
                            <TextBlock Text="Sleep Timeline" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>

                        <!-- Legend -->
                        <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,0,0,12" HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Ellipse Width="8" Height="8" Fill="#3F51B5" />
                                <TextBlock Text="Deep" FontSize="12" Opacity="0.7" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Ellipse Width="8" Height="8" Fill="#7C4DFF" />
                                <TextBlock Text="Light / Core" FontSize="12" Opacity="0.7" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Ellipse Width="8" Height="8" Fill="#00BCD4" />
                                <TextBlock Text="REM" FontSize="12" Opacity="0.7" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Ellipse Width="8" Height="8" Fill="#FF9800" />
                                <TextBlock Text="Awake" FontSize="12" Opacity="0.7" />
                            </StackPanel>
                        </StackPanel>

                        <chart:SfCartesianChart Height="200" IsTransposed="True">
                            <chart:SfCartesianChart.XAxes>
                                <chart:CategoryAxis ShowMajorGridLines="False" ShowLabel="False" ShowAxisLine="False" />
                            </chart:SfCartesianChart.XAxes>
                            <chart:SfCartesianChart.YAxes>
                                <chart:DateTimeAxis ShowMajorGridLines="True" LabelFormat="HH:mm" />
                            </chart:SfCartesianChart.YAxes>
                            
                            <chart:RangeColumnSeries 
                                ItemsSource="{x:Bind SleepData, Mode=OneWay}" 
                                XBindingPath="Category" 
                                High="EndDateTime" 
                                Low="StartDateTime"
                                Fill="{Binding ColorBrush}" />
                        </chart:SfCartesianChart>
                    </StackPanel>
                </Border>

                <!-- STEPS CHART -->
                <Border Background="{ThemeResource AppGlassColorBrush}" BorderBrush="{ThemeResource AppGlassBorderColorBrush}" BorderThickness="1" CornerRadius="16" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
                            <FontIcon Glyph="&#xE7EE;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="#FF9800" />
                            <TextBlock Text="Steps Today" FontSize="16" FontWeight="SemiBold" />
                        </StackPanel>

                        <chart:SfCartesianChart Height="200">
                            <chart:SfCartesianChart.XAxes>
                                <chart:DateTimeAxis ShowMajorGridLines="False" LabelFormat="HH:mm" />
                            </chart:SfCartesianChart.XAxes>
                            <chart:SfCartesianChart.YAxes>
                                <chart:NumericalAxis ShowMajorGridLines="True" />
                            </chart:SfCartesianChart.YAxes>

                            <chart:ColumnSeries ItemsSource="{x:Bind StepsData, Mode=OneWay}" XBindingPath="StartTime" YBindingPath="Value" Fill="#FF9800" />
                        </chart:SfCartesianChart>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
