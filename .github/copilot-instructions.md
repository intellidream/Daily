# Copilot Instructions

## Project Guidelines
- Store Syncfusion license key in `Configuration/Secrets.cs` for development wiring (production storage handled later).
- Implement debug UI changes as native MAUI XAML widgets in the main window (not Razor/Blazor components).